#- The "main" profile points here, so "aurora run" from the project root runs
#- this file with the default 8-byte tapes.

ident area = defer { feed(0) * feed(1); };

ident width = 30;
ident height = 20;

printb area(width, height);
